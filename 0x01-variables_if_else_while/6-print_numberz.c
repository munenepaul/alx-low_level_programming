#include <stdio.h>
#include <stdlib.h>
#include <time.h>
/**
 * main - Entry point
 * Return: always (0) sucess
 */

int main(void)

{
	int e;

	for (e = '0'; e <= '9'; e++)
	putchar(e);
	putchar('\n');

	return (0);
}
